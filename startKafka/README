Usage:

Start a cluster:
docker-compose up -d

Add more brokers:
docker-compose scale kafka=3

Destroy a cluster:
docker-compose stop

help:
https://hub.docker.com/r/wurstmeister/kafka/

config:
It might be needed to change the local network ip in the .yml file
